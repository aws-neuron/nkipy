"""Operation kernels and pure-math utilities.

Submodules:
  nn              — RMS norm, RoPE, SiLU, TP sharding utilities
  lm_head_sampling — reference sampling math (softmax, threshold, CDF)
  vocab_parallel_embedding — vocab-parallel embedding ops
  moe/            — mixture-of-experts ops
"""
